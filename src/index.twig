<style>
  .select2-container--default .select2-results__option--highlighted[aria-selected] {
        background-color: #5897fb !important;
    color: white !important;
  }
  .select2-container--default .select2-results__option--selected  {
            background-color: #193d74 !important;
    color: white !important;
}
  .cats-list a.website {
    font-size: 15px;
}
  .stats-box p {
    color:black;
}
  .bootstrap-select>.dropdown-toggle {
    height: 100%;
}
  .nav-tabs li a{
	cursor:pointer !important;
  }
  .text-white {
	color: #fff !important;
  }
    
  .filter-option-inner {
    height: 100%;
    display: flex;
    align-items: center;
  }
   body.dark #order-form .select2 *, body.dark .select2-container *, body.dark .bootstrap-select .dropdown-menu li a:hover span.text{
    color:#000 !important;
  }
    .filter-option-inner-inner {
    display: flex;
    align-items: center;
  }
	@import url('https://fonts.googleapis.com/css?family=Open+Sans:400,600,700,800|Cairo&display=swap');
	.stats-box h4 {
	font-size: 20px;
	font-weight: 400;
	color: #262f44;
	}
	.breadcrumb {
	background-color: transparent;
	border: none;
	margin: 0;
	}
	.breadcrumb > .active,.breadcrumb > li> a {
	color: #262f44;
	}
	.stats-box {
	background-color: #ffffff;
	box-shadow: 0px 10px 15px -10px rgba(0, 0, 0, 0.4);
	border-radius: 6px;
	padding: 20px;
	margin-bottom: 20px;
	}
	.stats-box.big-stats {
	min-height: 145px;
	display: flex;
	flex-direction: column;
	justify-content: center;
	}
	.stats-box p {
	margin: 0;
	font-size: 12px;
	}
	.stats-icon {
	margin-bottom: 12px;
	}
	.well {
	border: 1px solid #e7ebf6;
	background-color: #ffffff;
	box-shadow:0px 10px 15px -10px rgba(0, 0, 0, 0.4);
	}
	.well .form-control {
	border: 1px solid #e7ebf6;
	background-color: #ffffff;
	box-shadow: none;
	}
	.well-title {
	font-size: 22px;
	font-weight: 300;
	border-bottom: 1px solid #e7ebf6;
	padding-bottom: 12px;
	margin-bottom: 15px;
	}
	.well .control-label {
      font-family: Roboto,helvetica neue,Helvetica,Arial,sans-serif;
		font-size: 14px;
		font-weight: bold;
		color: #3e3f3a;
	}
    #description, select, input[type=text] {
      font-family: Roboto,helvetica neue,Helvetica,Arial,sans-serif;
  		font-size: 14px;
      font-weight: unset;
      color: #3e3f3a;
  	}
  
	.news-count {
	width: 42px;
	height: 42px;
	display: block;
	text-align: center;
	color: #ffffff;
	font-size: 20px;
	font-weight: 700;
	line-height: 39px;
	border-radius: 500px;
	position: absolute;
	left: 0;
	top: 0px;
	}
	.news-content h4 {
	font-size: 20px;
	font-weight: 400;
	margin-top: 0;
	}
	.date {
	font-size: 12px;
	font-weight: 300;
	color: #fa329c;
	position: absolute;
	right: 0;
	top: 0;
	}
	.news-item {
	position: relative;
	padding-left: 68px;
	margin-bottom: 19px;
	}
	body {
	background-image: url(https://i.imgur.com/WSMju6D.png);
	background-repeat: no-repeat;
	background-size: 105% 230px;
	background-position: -50px -40px;
	}
	.navbar.navbar-default.navbar-static-top {
	background-color: transparent;
	border: none;
	padding: 0;
	}
	.navbar-default .container-fluid {
	background-color: #fff;
	margin-left: 15px;
	margin-right: 15px;
	box-shadow: 0px 10px 15px -10px rgba(0, 0, 0, 0.4);
	border-radius: 6px;
	padding-top: 10px;
	padding-bottom: 10px;
	margin-top: 20px;
	}
  .cats-list{
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(18%,1fr));
    gap: 10px;
    list-style: none;
    width: 60%;
    margin: 20px 0;
    padding: 0;
  }
  .cats-list a {
    text-align: center;
    cursor: pointer;
    text-transform: capitalize;
    display: inline-block;
    width: 100%;
    padding: 12px 10px;
    font-size: 17px;
    background: #fff;
    color: #ffff;
    border-radius: 5px;
    font-weight: 600;
    position: relative;
    border: 3px solid;
}
.cats-list a.selected::after{
   content: "";
    position: absolute;
    width: 50px;
    height: 6px;
    background: linear-gradient(to right, rgba(250, 50, 157, 1) 0%, rgba(162, 13, 160, 1) 100%);
    border-radius: 40px;
    left: 50%;
    bottom: 0;
    transform: translateX(-50%);
}
.cats-list a.selected{
  background-color: #fff !important;
}

.cats-list a.facebook.selected{
  color: #256dc7;
}
.cats-list a.facebook{
  background: #256dc7;
}
.cats-list a.facebook.selected span{
  background: #256dc7 !important;
  background-clip: text !important;
    -webkit-background-clip: text !important;
}
.cats-list a.twitter.selected{
  color: #1491e8;
}
.cats-list a.twitter{
  background: #1491e8;
}
.cats-list a.twitter.selected span{
  background: #1491e8 !important;
  background-clip: text !important;
    -webkit-background-clip: text !important;
}
.cats-list a.youtube.selected{
  color: #f13319;
}
.cats-list a.youtube{
  background: #f13319;
}
.cats-list a.youtube.selected span{
  background: #f13319 !important;
  background-clip: text !important;
    -webkit-background-clip: text !important;
}
.cats-list a.tiktok.selected{
  color: #010101;
}
.cats-list a.tiktok{
  background: #010101;
}
.cats-list a.tiktok.selected span{
  background: #010101 !important;
  background-clip: text !important;
    -webkit-background-clip: text !important;
}
.cats-list a.instagram.selected{
  color: #ff0076;
}
.cats-list a.instagram{
  background: #ff0076;
}
.cats-list a.instagram.selected span{
  background: #ff0076 !important;
  background-clip: text !important;
    -webkit-background-clip: text !important;
}
.cats-list a.telegram.selected{
  color: #27a3e2;
}
.cats-list a.telegram{
  background: #27a3e2;
}
.cats-list a.telegram.selected span{
  background: #27a3e2 !important;
  background-clip: text !important;
    -webkit-background-clip: text !important;
}
.cats-list a.others.selected{
  color: #a20da0;
}
.cats-list a.others{
  background: #a20da0;
}
.cats-list a.others.selected span{
  background: #a20da0 !important;
  background-clip: text !important;
    -webkit-background-clip: text !important;
}
.cats-list a.everything.selected{
  color: #f6309c;
}
.cats-list a.everything{
  background: #f6309c;
}
.cats-list a.everything.selected span{
  background: #f6309c  !important;
  background-clip: text !important;
    -webkit-background-clip: text !important;
}

.cats-list a.website.selected{
  color: #333;
}
.cats-list a.website{
  background: #333;
}
.cats-list a.website.selected span{
  background: #333  !important;
  background-clip: text !important;
    -webkit-background-clip: text !important;
}

.filter-option-inner{
  text-transform: capitalize;

}

  .cats-list span{
    background: #fff !important;
    background-clip: text !important;
    -webkit-background-clip: text !important;
  }
  @media(max-width:1200px){
    .cats-list{
      width: 100%;
    }
  }
	@media(max-width:550px){
	body{
	background-image: none;
	}
  .cats-list{
    gap: 5px;
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    align-items: flex-start;
  }
  .cats-list a{
    padding: 5px;
    font-size: 12px;
    display: block;
  }
  .cats-list a span{
    display: block;
    height: 20px;
  }
  .cats-list li{
    min-width: 18%;
    height: 100%;
  }
  .cats-list a.selected::after{
    width: 30px;
  }
	.navbar-default .container-fluid {
	background-color: transparent;
	margin-left: 0;
	margin-right: 0;
	box-shadow: none;
	border-radius: 0;
	padding-top: 0;
	padding-bottom: 0;
	margin-top: 0;
	}
	.navbar.navbar-default.navbar-static-top {
	padding: 10px 0;
	background-color: #fff;
	box-shadow: 0 0 30px -10px rgba(0, 0, 0, 0.3);
	}
	.hideonmobile{
	display:none;
	}
	}
textarea {
	height:180px !important;
}
  
  
  p {
    color: #fff;
}

ul, ol {
    color: #fff;
}

.nightmode .id-boxi {
    background-color: #151428;
}
.update-content-wrap .id-boxi {
	font-weight: 600;
    color: #fff;
    background-image: url(https://i.imgur.com/WSMju6D.png);
    background-repeat: no-repeat;
    background-size: 105% 230px;
    background-position: 0;
}
.id-boxi {
	padding: 6px 17px;
    border-radius: 10px;
}
.update-content-wrap a.update.decreased {
    background: #333333;
    color: #555;
}
.update-content-wrap a.update {
    display: inline-block;
    margin-left: auto;
  background: #777;
}
.badge {
    display: inline-block;
    min-width: 10px;
    padding: 3px 7px;
    font-size: 12px;
    font-weight: bold;
    line-height: 1;
    color: #fff;
    text-align: center;
    white-space: nowrap;
    vertical-align: middle;
    background-color: #c0392b;
    border-radius: 20px !important;
}
.btn-sm, .btn-group-sm > .btn {
    padding: 5px 10px;
    font-size: 12px;
    line-height: 1.5;
    border-radius: 3px;
}
.btn {
    display: inline-block;
    padding: 6px 12px;
    margin-bottom: 0;
    font-size: 14px;
    font-weight: normal;
    line-height: 1.42857143;
    text-align: center;
    white-space: nowrap;
    vertical-align: middle;
    -ms-touch-action: manipulation;
    touch-action: manipulation;
    cursor: pointer;
    -webkit-user-select: none;
    -moz-user-select: none;
    -ms-user-select: none;
    user-select: none;
    border: 1px solid transparent;
    border-radius: 4px;
}
.update-content-wrap .update-ser-name {
    font-size: 14px;
    color: #555;
    margin: 8px 0;
    width: 100%;
}
.update-content-wrap .update-date {
    font-size: 10px;
    color: #555;
    font-weight: 600;
    padding-top: 9px;
    border-top: 1px solid #fff;
    display: block;
    width: 100%;
}
.update-content-wrap:first-child, .update-content-wrap:nth-child(2), .update-content-wrap:nth-child(3), .update-content-wrap:nth-child(4) {
    display: flex;
}
.update-content-wrap {
    display: none;
}
.update-content-wrap {
    background-color: #ffffff;
    box-shadow: 0px 10px 15px -10px rgb(0 0 0 / 40%);
    padding: 12px;
    border-radius: 12px;
    margin-bottom: 15px;
    display: flex;
    flex-wrap: wrap;
    align-items: center;
}
</style>

<div class="paddingbody"></div>
<div class="container-fluid container-fluid-spacious">
	<div class="row hideonmobile">
		<div class="col-sm-4">
			<div class="stats-box big-stats">
				<h4>Hello {{ user['username'] }}</h4>
				<ol class="breadcrumb">
					<li><a href="#">Home</a></li>
					<li class="active">New Order</li>
				</ol>
			</div>
		</div>
   
      
		<div class="col-sm-8">
			<div class="row">
				<div class="col-sm-3">
					<div class="stats-box text-center">
						<div class="stats-icon"><img src="https://i.imgur.com/Al1FEaX.png" /></div>
						<p>Total Orders</p>
						<h4>1</h4>
					</div>
				</div>
				<div class="col-sm-3">
					<div class="stats-box text-center">
						<div class="stats-icon"><img src="https://i.imgur.com/YjtIHuD.png" /></div>
						<p>Account Spendings</p>
						<h4>$0</h4>
					</div>
				</div>
				<div class="col-sm-3">
					<div class="stats-box text-center">
						<div class="stats-icon"><img src="https://i.imgur.com/IMIGWvd.png" /></div>
						<p>First Deposit Bonus</p>
						<h4>5%</h4>
					</div>
				</div>
				<div class="col-sm-3">
					<div class="stats-box text-center">
						<div class="stats-icon"><img src="https://i.imgur.com/j5HfHaL.png" /></div>
						<p>Account Status</p>
						<h4 class="account_status_span">{{ user['username'] }}</h4>
					</div>
				</div>
			</div>
		</div>
	</div>
    <div class="row"><div class="col-md-12">
    <ul class='cats-list'>
      {% set uniqueCats = ["youtube","facebook","tiktok","others","everything","instagram","telegram","twitter","website traffic"] %}
          {% for categoryName in uniqueCats %}
          <li><a href='#' data-value="{{ categoryName }}" class="{{ categoryName }}"> {{ categoryName }}</a></li>
          {%endfor%}
    </ul>
</div></div>
	<div class="row">
		<div class="col-sm-8">
<div class="new-order-form" style="opacity:0;transition: opacity 0.4s ease-in-out">
         <div class="d-flex">
           <ul id="myTabs000"  class="video-tabs nav nav-tabs dash-tabs" role="tablist" data-tabs="tabs">
         <li class="active"><a id="neworders" data-target="#tab-new" class="btn tab-head mr-3 btn-primary text-white">+ NEW ORDER</a></li>
        <li class="active"><a id="favourites" data-target="#tab-favorite" class="btn tab-head mr-3 btn-dark">MY FAVORITE</a></li>
      </ul>
         </div>
  <div>
  
  {% if error %}
            <div class="alert alert-dismissible alert-danger {% if site['rtl'] %} rtl-alert {% endif %}">
              <button type="button" class="close" data-dismiss="alert">&times;</button>
              {{ errorMessage }}
            </div>
          {% endif %}

          {% if 1 == success %}
    		<script>
             setTimeout(() => {
               console.log('auto select fav service working')
               FavController.initSelectCategoryServiceOussail(sessionStorage.getItem('serviceId'));
               
             },1000);
    </script>  
            <div class="alert alert-success alert-dismissible" role="alert">
              <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
              <h4>{{lang('neworder.message.success')}}</h4>
              {{lang('neworder.id')}}: {{order['id']}}<br>
              {{lang('neworder.service')}}: {{order['service']}}
              <br>
              {{lang('neworder.link')}}: {{order['link']}}<br>
              {{lang('neworder.quantity')}}: {{order['quantity']}}<br>
              {{ lang('neworder.charge') }}: {% if order.converted %} <span data-toggle="tooltip" data-placement="top" title="{{ order.original_charge }}">{{ order['charge'] }}</span> {% else %} {{ order['charge'] }} {% endif %}<br>
              {{ lang('neworder.balance') }}: {% if order.converted %} <span data-toggle="tooltip" data-placement="top" title="{{ order.original_balance }}">{{ order['balance'] }}</span> {% else %} {{ order['balance'] }} {% endif %}<br>
            </div>
          {%endif%}

          {% if 2 == success %}
    <script>
             setTimeout(() => {
               console.log('auto select fav service working')
               FavController.initSelectCategoryServiceOussail(sessionStorage.getItem('serviceId'));
               
             },1000);
    </script>
            <div class="alert alert-success alert-dismissible" role="alert">
              <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
              <h4>{{lang('neworder.message.success')}}</h4>
              {{lang('neworder.subscription_id')}}: {{order['id']}}<br>
              {{lang('neworder.service')}}: {{order['service']}}
              <br>
              {{lang('neworder.username')}}: {{order['link']}}<br>
              {{lang('neworder.quantity')}}:
              {% if order['quantity_min'] == order['quantity_max'] %}
                {{order['quantity_max']}}
              {% else %}
                {{order['quantity_min']}}-{{order['quantity_max']}}
              {%endif%}
              <br>
              {{lang('neworder.new_posts')}}:
              {% if 0 == order['posts'] %}
                &#8734;
              {% else %}
                {{order['posts']}}
              {%endif%}
              <br>
              {{lang('neworder.delay')}}:
              {{order['delay']}}
              <br>
            </div>
          {%endif%}
  
  
  </div>
         <style>
            .hidden.fields { display: none; }
         </style>
         
         <div id="tab-new" class="tab-content" style="display: block;">
      <div class="well">
        <form {% if site['rtl'] %} class="rtl-form"{% endif %} action="{{ page['url'] }}" method="post" id="order-form">
          {% if categories %}
            <div class="form-group">
              <label for="orderform-category" class="control-label">{{ lang('neworder.category') }}</label>
              <select class="form-control" id="orderform-category" name="OrderForm[category]">
                {% for categoryId,categoryName in categories %}
                  <option value="{{ categoryId }}" {% if categoryId == data['category'] %} selected {%endif%}>{{ categoryName }}</option>
                {%endfor%}
              </select>
            </div>
          {% endif %}
			
          <div class="form-group">
            <label for="service" class="control-label">{{ lang('neworder.service') }}<br>
            <span id="orderServiceId" style="color: #7d7d7d;font-size: 12px;">ID : 13</span>
            </label>
                        <span style="cursor: pointer;" id="currentServiceId" class="fav" data-id="0"><i class="far fa-star"></i></span>

            <div></div>
            <select id="orderform-service" class="form-control" id="service" name="OrderForm[service]"></select>
          </div>

          <div class="form-group hidden fields" id="service_description">
            <label for="service_description" class="control-label">{{ lang('neworder.description') }}</label>
            <div class="panel-body border-solid border-rounded">
                {{service['description']}}
            </div>
          </div>

          <div id="fields"></div>

          <div class="form-group">
            <label for="charge" class="control-label">{{ lang('neworder.charge') }}</label>
            <input type="text" class="form-control" id="charge" value="{{ order['charge'] }}" readonly>
          </div>

          {% if check_agreement %}
            <div class="form-group">
              <label class="control-label terms">
                <input type="checkbox"  name="OrderForm[termsofservice]" class="terms-accept-checkbox" value="1" id="terms" {{data['termsofservice'] ? 'checked' : ''}}> {{lang('signup.accept_terms_text')}}
                <a href="{{ page_url('terms') }}" target="_blank">{{ lang('terms.signup_checkbox') }}</a>
              </label>
            </div>
          {%endif%}

          <input type="hidden" name="_csrf" value="{{ csrftoken }}">
          <button type="submit" class="btn btn-primary">{{ lang('neworder.button') }}</button>
        </form>
      </div>
{% if newOrderText %}
  <div class="container">
    <div class="row">
      <div class="col-md-8 col-md-offset-2">
        <div class="well {% if site['rtl'] %} rtl-content {% endif %}">
          {{ newOrderText }}
        </div>
      </div>
    </div>
  </div>
{% endif %}
         </div>
         <div id="tab-favorite" class="tab-content" style="display:none;">
           <div class="well">
            <form action="/" method="post">
               <div class="form-group">
              <label for="fav_category" class="control-label">Category</label>
                  <select id="fav_category" class="form-control" name="OrderForm[category]"></select>
               </div>
               <div class="form-group">
                 <label for="fav_service" class="control-label">Service</label>

                  <select id="fav_service" class="form-control" name="OrderForm[service]"></select>
               </div>
               <div id="fav_fields" class="form-group"></div>

               <input type="hidden" name="_csrf" value="{{ csrftoken }}">
				<button type="submit" class="btn btn-primary">{{ lang('neworder.button') }}</button>
            </form>
             </div>
         </div>
      </div>
		</div>
		<div class="col-sm-4 hideonmobile" style="display: none;">
   <div class="">
      <ul id="myTabs" class="video-tabs nav nav-tabs dash-tabs" role="tablist" data-tabs="tabs">
         <li class="active"><a href="#abc" data-toggle="tab" aria-expanded="true"><i class="fa fa-bell"></i> CHEAPEST SERVICES  </a></li>
      </ul>

      <br>
      <div class="update-content-wrap">
         <div class="id-boxi">0.80$</div>
         <a href="javascript:void(0)" style="font-family: cairo,'Font Awesome 5 Brands';background: #ff1800;color: #ffffff;" class="btn btn-sm update badge increased"> New Service
         <i class="" aria-hidden="true"></i></a>
         <div class="update-ser-name">
           914 - YouTube Views internal YouTube source [100% non-drop] ~ R30 ~ [additional likes]	
         </div>
      </div>
      <div class="update-content-wrap">
         <div class="id-boxi">0.63$</div>
         <a href="javascript:void(0)" style="font-family: cairo,'Font Awesome 5 Brands';background: #ff1800;color: #ffffff;" class="btn btn-sm update badge increased"> New Service
         <i class="" aria-hidden="true"></i></a>
         <div class="update-ser-name">
            935 - YouTube Watch Time | From Suggested | 🔴Put Video 60 Mins | Speed 500 | Lifetime Refill		
         </div>
      </div>
      <div class="update-content-wrap">
         <div class="id-boxi">33.49$</div>
         <a href="javascript:void(0)" style="font-family: cairo,'Font Awesome 5 Brands';background: #ff1800;color: #ffffff;" class="btn btn-sm update badge increased"> New Service
         <i class="" aria-hidden="true"></i></a>
         <div class="update-ser-name">
            918 - 🤖YouTube Subscribers | 🔥High Drop | ⚡Speed +1K | 🚫No Guarantee
         </div>
      </div>
      <ul id="myTabs" class="video-tabs nav nav-tabs dash-tabs" role="tablist" data-tabs="tabs"></ul>
      <div class="update-content-wrap">
         <div class="id-boxi">$</div>
         <a href="javascript:void(0)" style="font-family: cairo,'Font Awesome 5 Brands';background: #004eff;color: #ffffff;" class="btn btn-sm update badge increased"> New Service
         <i class="" aria-hidden="true"></i></a>
         <div class="update-ser-name">
            0 - 🤖Facebook Video Views | 🔥High Drop | ⚡Speed +1K | 🚫No Guarantee
         </div>
      </div>
      <div class="update-content-wrap">
         <div class="id-boxi">$</div>
         <a href="javascript:void(0)" style="font-family: cairo,'Font Awesome 5 Brands';background: #004eff;color: #ffffff;" class="btn btn-sm update badge increased"> New Service
         <i class="" aria-hidden="true"></i></a>
         <div class="update-ser-name">
            0 - 🤖Facebook Post Likes | 🔥High Drop | ⚡Speed +1K | 🚫No Guarantee
         </div>
      </div>
      <div class="update-content-wrap">
         <div class="id-boxi">1.30$</div>
         <a href="javascript:void(0)" style="font-family: cairo,'Font Awesome 5 Brands';background: #004eff;color: #ffffff;" class="btn btn-sm update badge increased"> New Service
         <i class="" aria-hidden="true"></i></a>
         <div class="update-ser-name">
            933 - 🤖Facebook Page Likes | 🔥High Drop | ⚡Speed +1K | 🚫No Guarantee
         </div>
      </div>
      <ul id="myTabs" class="video-tabs nav nav-tabs dash-tabs" role="tablist" data-tabs="tabs"></ul>
      <div class="update-content-wrap">
         <div class="id-boxi">$0.70</div>
         <a href="javascript:void(0)" style="font-family: cairo,'Font Awesome 5 Brands';background: #ff00f7;color: #ffffff;" class="btn btn-sm update badge increased"> New Service
         <i class="" aria-hidden="true"></i></a>
         <div class="update-ser-name">
            154 - NEW Instagram Real Followers [150K] [10/20K Day - 30 Days Refill ]	
         </div>
      </div>
      <div class="update-content-wrap">
         <div  class="id-boxi">0.63$</div>
         <a href="javascript:void(0)" style="font-family: cairo,'Font Awesome 5 Brands';background: #ff00f7;color: #ffffff;" class="btn btn-sm update badge increased"> New Service
         <i class="" aria-hidden="true"></i></a>
         <div class="update-ser-name">
            112 - NEW Instagram Followers [50K] [10K/Day - 30 Days Auto Refill]	
         </div>
      </div>
      <div class="update-content-wrap">
         <div class="id-boxi">$0.77</div>
         <a href="javascript:void(0)" style="font-family: cairo,'Font Awesome 5 Brands';background: #ff00f7;color: #ffffff;" class="btn btn-sm update badge increased"> New Service
         <i class="" aria-hidden="true"></i></a>
         <div class="update-ser-name">
            1089 - Instagram Followers [365 Days Refill]	
         </div>
      </div>
   </div>
</div>
	</div>
</div>

<script>
  $(function () {
    var langAttr = $("html").attr("lang");
    if(langAttr === 'ar'){
      var catList = document.querySelector('.cats-list');
      if(catList){
        catList.parentNode.setAttribute('dir', 'rtl');
        var catsList = catList.querySelectorAll('li a');
        var arabicCats = {
          "Youtube": "يوتيوب",
          "Facebook": "فيسبوك",
          "Tiktok": "تيك توك",
          "Others": "أخرى",
          "Everything": "كل شيء",
          "Instagram": "إنستغرام",
          "Telegram": "تيليجرام",
          "Twitter": "تويتر",
          "Website Traffic": "زوار المواقع"
          };

        if(catsList.length > 0){
          catsList.forEach(cat => {
            var text = cat.innerText;
            //console.log(text,arabicCats[text.trim()], arabicCats["Youtube"]);
            cat.innerText = arabicCats[text.trim()];
          })
        }
      }

      var actionsBtns = document.querySelectorAll('.new-order-form .nav-tabs li a');
      
      if(actionsBtns.length > 0){
          var arabicBtns = {
          "Youtube": "يوتيوب",
          "Facebook": "فيسبوك",
          };
          actionsBtns.forEach(actionBtn => {
            var text = actionBtn.innerText.toLowerCase();
            actionBtn.parentNode.style.float="right";
            if (text.includes("new order")) {
                actionBtn.innerText = text.replace(/new order/gi, "طلب جديد");
            }
            if (text.includes("my favorite")) {
                actionBtn.innerText = text.replace(/my favorite/gi, "المفضل لدي");
            }
          })
        }

        var newOrderForm = document.querySelector('.new-order-form');
        if(newOrderForm){
          newOrderForm.parentNode.style.float = "right";
        }
    }
     $('#orderform-service').select2({
      dir:langAttr === 'ar' ? "rtl" : 'ltr',
      templateResult: formatOption,
      templateSelection: formatSelection
    });

    function formatOption(option) {
      if (!option.id) {
        return option.text;
      }
      var icon = getIcon(option.text);
      return $(`
        <span>
          <span class="${icon}"></span>
          ${option.text}
        </span>
      `);
    }

    function formatSelection(option) {
      if (!option.id) {
        return option.text;
      }
      
      var icon = getIcon(option.text);
      return $(`
        <span>
          <span class="${icon}"></span>
          ${option.text}
        </span>
      `);
    }

    function getIcon(elementTxt) {
      var catsObj = {
        "يوتيوب": "youtube",
        "فيسبوك": "facebook",
        "تيك توك": "tiktok",
        "آخر": "others",
        "كل شئ": "everything",
        "انستقرام": "instagram",
        "انستغرام": "instagram",
        "تيلجرام": "telegram",
        "تويتر": "twitter",
        "زيارات الموقع": "website traffic",
        "لينكد ان": "linkedin",
        "سبوتيفاي": "spotify",
        "سناب شات":"snapchat",
        "بينترست":"pinterest",
    };
    var elementTxt = elementTxt.toLowerCase();
    var foundKey = '';
    for (var key in catsObj) {
        if (elementTxt.includes(key)) {
            foundKey = key;
            break;
        }
    }

      if (elementTxt.includes('snapchat') || catsObj[foundKey] === 'snapchat') {
        return 'fab fa-snapchat-square';
      } else if (elementTxt.includes('facebook') || catsObj[foundKey] === 'facebook') {
        return 'fab fa-facebook';
      } else if (elementTxt.includes('twitter') || catsObj[foundKey] === 'twitter') {
        return 'fab fa-twitter';
      } else if (elementTxt.includes('instagram') || catsObj[foundKey] === 'instagram') {
        return 'fab fa-instagram';
      } else if (elementTxt.includes('youtube') || catsObj[foundKey] === 'youtube') {
        return 'fab fa-youtube';
      } else if (elementTxt.includes('telegram') || catsObj[foundKey] === 'telegram') {
        return 'fab fa-telegram';
      } else if (elementTxt.includes('spotify') || catsObj[foundKey] === 'spotify') {
        return 'fab fa-spotify';
      } else if (elementTxt.includes('linkedin') || catsObj[foundKey] === 'linkedin') {
        return 'fab fa-linkedin';
      } else if (elementTxt.includes('tiktok') || catsObj[foundKey] === 'tiktok') {
        return 'fab fa-tiktok';
      } else if (elementTxt.includes('pinterest') || catsObj[foundKey] === 'pinterest') {
        return 'fab fa-pinterest';
      } else if (elementTxt.includes('tumblr')) {
        return 'fab fa-tumblr-square';
      } else if (elementTxt.includes('vimeo')) {
        return 'fab fa-vimeo';
      } else if (elementTxt.includes('discord')) {
        return 'fab fa-discord';
      } else if (elementTxt.includes('website') || catsObj[foundKey] === 'website traffic') {
        return 'far fa-globe';
      } else if (elementTxt.includes('dailymotion')) {
        return 'fab fa-dailymotion';
      } else if (elementTxt.includes('onlyfan')) {
        return 'far fa-lock-alt';
      } else if (elementTxt.includes('quora')) {
        return 'fab fa-quora';
      } else if (elementTxt.includes('club')) {
        return 'fas fa-camera-home';
      } else if (elementTxt.includes('others')) {
        return 'fas fa-star-of-life';
      } else if (elementTxt.includes('everything')) {
        return 'fas fa-list';
      }
    }

    $('#orderform-category').selectpicker();
      
    setTimeout(() => {
      addIconsSelector('.dropdown-menu a .text');
  		addIconsSelector('.filter-option-inner-inner');
      addIconsSelector('#orderform-category option');
    }, 100);
  });
  function sleep (time) {
    return new Promise((resolve) => setTimeout(resolve, time));
  }
  
    function addIconsSelector(selector) {
    var elements = document.querySelectorAll(selector);
    
    if(!elements.length) {
      sleep(100).then(() => {
        addIconsSelector(selector);
      });
      return;
    }
for(let i = 0; i < elements.length; i++) {
    if(elements[i].getElementsByTagName('span').length == 0){
if(elements[i]) {
    var catsObj = {
        "يوتيوب": "youtube",
        "فيسبوك": "facebook",
        "تيك توك": "tiktok",
        "آخر": "others",
        "كل شئ": "everything",
        "انستقرام": "instagram",
        "انستغرام": "instagram",
        "تيلجرام": "telegram",
        "تويتر": "twitter",
        "زيارات الموقع": "website traffic",
        "لينكد ان": "linkedin",
        "سبوتيفاي": "spotify",
        "سناب شات":"snapchat",
        "بينترست":"pinterest",
        "خدمات جديدة": "new services"
    };
    var elementTxt = elements[i].innerText.toLowerCase();
    var foundKey = '';
    for (var key in catsObj) {
        if (elementTxt.includes(key)) {
            foundKey = key;
            break;
        }
    }
    var arabicRegex = /[\u0600-\u06FF]/;
    if(arabicRegex.test(elementTxt)){
        elements[i].parentNode.style.textAlign = 'right';
    }
    
    
    if(elementTxt.includes('snapchat') || catsObj[foundKey] === 'snapchat') {
        elements[i].innerHTML =  '<span class="fab fa-snapchat-square"></span> ' + elements[i].innerHTML;
    }else if (elementTxt.includes('new services') || elementTxt.includes('خدمات جديدة')) {
        if ($(elements[i]).find('img[src="https://i.ibb.co/rdJ7MCd/Whats-App-Image-2023-02-15-at-3-22-10-AM-removebg-preview.png"]').length === 0) {
          elements[i].innerHTML =  '<img style="width: 20px; margin: 0 5px;" src="https://i.ibb.co/rdJ7MCd/Whats-App-Image-2023-02-15-at-3-22-10-AM-removebg-preview.png" alt="Whats-App-Image-2023-02-15-at-3-22-10-AM" border="0" />' + elements[i].innerHTML;
        }
    } else if (elementTxt.includes('facebook') || catsObj[foundKey] === 'facebook') {
        elements[i].innerHTML =  '<span class="fab fa-facebook"></span> ' + elements[i].innerHTML;
    } else if (elementTxt.includes('twitter') || catsObj[foundKey] === 'twitter') {
        elements[i].innerHTML =  '<span class="fab fa-twitter"></span> ' + elements[i].innerHTML;
    } else if (elementTxt.includes('instagram') || catsObj[foundKey] === 'instagram') {
        elements[i].innerHTML =  '<span class="fab fa-instagram"></span> ' + elements[i].innerHTML;
        if(selector === '#orderform-service option'){
          console.log('from here',elements[i].innerHTML);
        }
    } else if (elementTxt.includes('youtube') || catsObj[foundKey] === 'youtube') {
        elements[i].innerHTML =  '<span class="fab fa-youtube"></span> ' + elements[i].innerHTML;
    } else if (elementTxt.includes('telegram') || catsObj[foundKey] === 'telegram') {
        elements[i].innerHTML =  '<span class="fab fa-telegram"></span> ' + elements[i].innerHTML;
    } else if (elementTxt.includes('spotify') || catsObj[foundKey] === 'spotify') {
        elements[i].innerHTML =  '<span class="fab fa-spotify"></span> ' + elements[i].innerHTML;
    } else if (elementTxt.includes('linkedin') || catsObj[foundKey] === 'linkedin') {
        elements[i].innerHTML =  '<span class="fab fa-linkedin"></span> ' + elements[i].innerHTML;
    } else if (elementTxt.includes('tiktok') || catsObj[foundKey] === 'tiktok') {
        elements[i].innerHTML =  '<span class="fab fa-tiktok"></span> ' + elements[i].innerHTML;
    } else if (elementTxt.includes('pinterest') || catsObj[foundKey] === 'pinterest') {
        elements[i].innerHTML =  '<span class="fab fa-pinterest"></span> ' + elements[i].innerHTML;
    } else if (elementTxt.includes('tumblr')) {
        elements[i].innerHTML =  '<span class="fab fa-tumblr-square"></span> ' + elements[i].innerHTML;
    } else if (elementTxt.includes('vimeo')) {
        elements[i].innerHTML =  '<span class="fab fa-vimeo"></span> ' + elements[i].innerHTML;
    } else if (elementTxt.includes('discord')) {
        elements[i].innerHTML =  '<span class="fab fa-discord"></span> ' + elements[i].innerHTML;
    } else if (elementTxt.includes('website') || catsObj[foundKey] === 'website traffic') {
        elements[i].innerHTML =  '<span class="far fa-globe"></span> ' + elements[i].innerHTML;
    } else if (elementTxt.includes('dailymotion')) {
        elements[i].innerHTML =  '<span class="fab fa-dailymotion"></span> ' + elements[i].innerHTML;
    }
    else if (elementTxt.includes('onlyfan')) {
        elements[i].innerHTML =  '<span class="far fa-lock-alt"></span> ' + elements[i].innerHTML;
    } else if (elementTxt.includes('quora')) {
        elements[i].innerHTML =  '<span class="fab fa-quora"></span> ' + elements[i].innerHTML;
    } else if (elementTxt.includes('club')) {
        elements[i].innerHTML =  '<span class="fas fa-camera-home"></span> ' + elements[i].innerHTML;
    }
    else if (elementTxt.includes('others')) {
        elements[i].innerHTML =  '<span class="fas fa-star-of-life"></span> ' + elements[i].innerHTML;
    }
    else if (elementTxt.includes('everything')) {
        elements[i].innerHTML =  '<span class="fas fa-list"></span> ' + elements[i].innerHTML;
    }
}

}


}


}

</script>


<script type="text/javascript">
const orderServiceSelect = document.getElementById('orderform-service');

if(orderServiceSelect) {
setSelectedId(orderServiceSelect.value);
orderServiceSelect.addEventListener('change', function(e) {
    setSelectedId(e.target.value);
  hightLightFavorite(favServices);
});
}
const orderCategorySelect = document.getElementById('orderform-category');
if(orderCategorySelect) {
  
orderCategorySelect.addEventListener('change', function(e) {
    setTimeout(() => {
	setSelectedId(orderServiceSelect.value);
      hightLightFavorite(favServices);
}, 200);
    
});
}
 
function setSelectedId(id) {
  const orderServiceId =  document.getElementById('orderServiceId');
  const currentServiceId =  document.getElementById('currentServiceId');
  
if(orderServiceId && orderServiceSelect) {
	orderServiceId.innerText = 'ID : ' + id;
  currentServiceId.setAttribute('data-id', id);
}
}
  
setTimeout(() => {
  
 if(orderCategorySelect) {
	setSelectedId(orderServiceSelect.value);
   hightLightFavorite(favServices);
} 
}, 300);

</script>
<script>
  var categories = [
     {% for categoryId,categoryName in categories %}
     {id:{{ categoryId }},name:'{{ categoryName }}'},
                {%endfor%}
                        
                  ];

   var favServices = {% if user['custom_variable_1'] %} [{{ user['custom_variable_1'] }}] {% else %} [] {% endif %};
   
        jQuery('.tab-head').on('click', function() {
         jQuery('.tab-content').hide();
         $target = jQuery(this);
         sessionStorage.setItem("lasttab", $target.get(0).getAttribute('id'));

         jQuery('.tab-head').removeClass('btn-primary');
         jQuery('.tab-head').removeClass('text-white');
         jQuery('.tab-head').removeClass('btn-dark');

         jQuery('.tab-head').each(function(index) {
         if (jQuery(this).is($target)) {
            jQuery(this).addClass('btn-primary');
           	jQuery(this).addClass('text-white');
         } else {
            jQuery(this).addClass('btn-dark');
         }
         });
         jQuery($(this).data('target')).show();
      });                                                                                                 
                                                                                                         
   </script>

<script>
    var notifServices = [];
  
    var serviceList = [];

    $(document).ready(function(){
      hightLightFavorite(favServices);

      $('.fav').click(function () {
        var status = $(this).get(0).getAttribute('status');
        var sid = parseInt($(this).get(0).getAttribute('data-id'));
        $(this).html('<img class="img_ajax" src="https://s7.gifyu.com/images/ajax.gif">');
        doUpdateFavorite(this,status,sid);
      })
      
       $('#favourites').click(function () {
        FavController.initFavorite();
      });
      
      
      
      const savedTab = sessionStorage.getItem("lasttab");
      const tabId = savedTab ? savedTab : 'neworders';
      const tab = document.querySelector('#'+tabId);
      if(tab) {
		tab.click();
      }
      
       const tabcontainer = document.querySelector('.new-order-form');
      if(tabcontainer) {
		tabcontainer.style.opacity = '1';
      }
    });
  
  

    

    function doUpdateFavorite(obj,status,sid) {
      if(status == "1"){
       console.log('status', status);
        for (var i = 0; i < favServices.length; i++) {
          if (sid == favServices[i]) {
            favServices.splice(i, 1);
          }
        }
      }else{
        favServices.push(sid);
      }
      
      if(favServices.length) {
		favServices.filter((n) => { n != 000 });
      } else {
		favServices.push(000);
      }
      console.log('favServices', favServices);
      

      $.ajax({
        type: 'GET',
        url: '/changevariable/custom_variable_1',
        crossDomain: true,
        data:  'value='+favServices.toString(),

        success: function(responseData, textStatus, jqXHR) {
          $('.img_ajax').remove();
			
          if(status == "1"){
            $(obj).html("<i class='fas fa-star'></i>");
             $(obj).get(0).setAttribute('status', 1);
            
          }else{
            $(obj).html("<i class='far fa-star'></i>");
            $(obj).get(0).setAttribute('status', 0);
            showAddToFavourite();
          }
          hightLightFavorite(favServices);
        },
        error: function (responseData, textStatus, errorThrown) {
          console.log(responseData,textStatus,errorThrown);

          $('.img_ajax').remove();
          if(status == "1"){
            $(obj).html("<i class='fas fa-star'></i>");
            $(obj).get(0).setAttribute('status', 1);
            
          }else{
            $(obj).html("<i class='far fa-star'></i>");
            $(obj).get(0).setAttribute('status', 0);
          }
          hightLightFavorite(favServices);
        }
      });
    }

    function hightLightFavorite(data) {
      $('.fav').each(function () {
        var id = parseInt($(this).get(0).getAttribute('data-id'));
        if(data.indexOf(id) != -1){
          $(this).html("<i class='fas fa-star'></i>");
          $(this).get(0).setAttribute('status', 1);
        } else {
          $(this).html("<i class='far fa-star'></i>");
          $(this).get(0).setAttribute('status', 0);
        }
      })
    }
  

  function removeEmptyCategory() {
    $('.category').each(function () {
        var next = $(this).next();
        var services = $(this).nextUntil('.category');
        var empty = true;
        services.each(function() {
          if (!$(this).hasClass('hidden')) empty = false;
        });
        if (empty) $(this).addClass('hidden');
    })
  }


function showAddToFavourite() {
  console.log('khadam');
  // Get the snackbar DIV
  var x = document.getElementById("snackbar");

  // Add the "show" class to DIV
  x.className = "show";

  // After 3 seconds, remove the show class from DIV
  setTimeout(function(){ x.className = x.className.replace("show", ""); }, 3000);
}

</script>
<script>
  addIconsSelector('.cats-list a');
  $(".cats-list a").click(function(e){
    e.preventDefault();
    var catsObj = {
        "يوتيوب": "youtube",
        "فيسبوك": "facebook",
        "تيك توك": "tiktok",
        "آخر": "others",
        "كل شئ": "everything",
        "انستقرام": "instagram",
        "انستغرام": "instagram",
        "تيلجرام": "telegram",
        "تويتر": "twitter",
        "زيارات الموقع": "website traffic"
    };

    $(".cats-list a").removeClass("selected");
    $(this).addClass("selected");
    var val = $(this).data('value').trim().split(' ')[0];
    var cats = ["youtube","facebook","tiktok","twitter","instagram","telegram","website"];
    $('#orderform-category option').hide().removeClass().addClass("hide");
    if(val =="everything"){
      $('#orderform-category option').show().removeClass().addClass("show");
    }else if(val=="others"){
      $('#orderform-category option').show().removeClass().addClass("show");
      $('#orderform-category option').filter(function(){
        var st = this.textContent.trim().split(' ')[0];
        var contentVal = this.textContent.trim();
        var found = false;
        for (var key in catsObj) {
        if (contentVal.includes(key) || contentVal.includes(catsObj[key])) {
            found = true;
            break;
        }
        }
        if(cats.includes(st.toLowerCase()) || found){
          return st
        }
      }).hide().removeClass().addClass("hide");
    }
    else{
      $('#orderform-category option').filter(function(){
        var uniqeVal = this.textContent.trim().split(' ')[0].toLowerCase();
        var contentVal = this.textContent;
        var foundKey = '';
        for (var key in catsObj) {
            if (contentVal.includes(key)) {
                foundKey = key;
                break;
            }
        }
        return uniqeVal == val || (catsObj[foundKey] == val)
      }).show().removeClass().addClass("show");
    }
    $('#orderform-category').val($('#orderform-category option.show:first').val());
    $('#orderform-category').trigger('change');
    $('#orderform-category').selectpicker('refresh');

    setTimeout(() => {
      addIconsSelector('.dropdown-menu a .text');
  		addIconsSelector('.filter-option-inner-inner');
      addIconsSelector('#orderform-category option:not([style="display:none"])');
    }, 100);
    
 });
  function extractLastNumber(str) {
  const pattern = /-(\d+)$/;
  const matches = str.match(pattern);
  const number = matches[1];
  return number;
}
window.onload = () =>{
        setSelectedId('13');
  let options = [];
  document.querySelector(".select2").addEventListener("click", () => {
    options = document.querySelectorAll("#select2-orderform-service-results li");
    console.log(options);
    options.forEach((op) => {
      op.addEventListener("touchstart", (e) => {
        setSelectedId(
          extractLastNumber(e.currentTarget.getAttribute("data-select2-id"))
        );
      });
      op.addEventListener('mouseleave', (e) => {
        setSelectedId(
          extractLastNumber(e.currentTarget.getAttribute("data-select2-id"))
        );
      });
    });
  });
  
}

</script>
<div id="snackbar">Added to Favourites</div>

<style>
/* The snackbar - position it at the bottom and in the middle of the screen */
#snackbar {
  visibility: hidden; /* Hidden by default. Visible on click */
  min-width: 250px; /* Set a default minimum width */
  margin-left: -125px; /* Divide value of min-width by 2 */
  background-color: #333; /* Black background color */
  color: #fff; /* White text color */
  text-align: center; /* Centered text */
  border-radius: 2px; /* Rounded borders */
  padding: 16px; /* Padding */
  position: fixed; /* Sit on top of the screen */
  z-index: 1; /* Add a z-index if needed */
  left: 50%; /* Center the snackbar */
  bottom: 30px; /* 30px from the bottom */
}

/* Show the snackbar when clicking on a button (class added with JavaScript) */
#snackbar.show {
  visibility: visible; /* Show the snackbar */
  /* Add animation: Take 0.5 seconds to fade in and out the snackbar.
  However, delay the fade out process for 2.5 seconds */
  -webkit-animation: fadein 0.5s, fadeout 0.5s 2.5s;
  animation: fadein 0.5s, fadeout 0.5s 2.5s;
}

/* Animations to fade the snackbar in and out */
@-webkit-keyframes fadein {
  from {bottom: 0; opacity: 0;}
  to {bottom: 30px; opacity: 1;}
}

@keyframes fadein {
  from {bottom: 0; opacity: 0;}
  to {bottom: 30px; opacity: 1;}
}

@-webkit-keyframes fadeout {
  from {bottom: 30px; opacity: 1;}
  to {bottom: 0; opacity: 0;}
}

@keyframes fadeout {
  from {bottom: 30px; opacity: 1;}
  to {bottom: 0; opacity: 0;}
}

</style>
